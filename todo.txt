1. Словарь paths перенести в класс Config.
2. В DataPreprocessor.run() в начале обнулять все списки (images_list и др),
иначе в списках будут повторяться записи при повторных вызовах run().
3. Скорей всего обучение на фолдах не потребуется, поэтому функции prepare_loaders()
и prepare_test_loader() стоит объединить в одну.
4. В ModelWrapper.__init__() передавать параметр pretrained, чтобы не грузить imagenet
веса для случая, когда модель инициализируется из чекпоинта.